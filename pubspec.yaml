name: abstract_editor
description: A new Flutter project.
version: 0.0.3
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  uuid: ^3.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: